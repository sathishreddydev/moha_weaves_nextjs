name: Deploy to Production

on:
  workflow_dispatch:
    inputs:
      confirm_deployment:
        description: 'Type "deploy-production" to confirm production deployment'
        required: true
        default: ''

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Confirm Deployment
        run: |
          if [ "${{ github.event.inputs.confirm_deployment }}" != "deploy-production" ]; then
            echo "❌ Deployment not confirmed. Type 'deploy-production' to proceed."
            exit 1
          fi
          echo "✅ Production deployment confirmed. Proceeding..."

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run database migrations
        # DATABASE_URL is injected directly from secrets — template env file is NOT
        # copied because it contains placeholder values, not real credentials.
        run: npx drizzle-kit migrate
        env:
          NODE_ENV: production
          DATABASE_URL: ${{ secrets.PROD_DATABASE_URL }}

      - name: Build application
        run: npm run build
        env:
          NODE_ENV: production
          DATABASE_URL: ${{ secrets.PROD_DATABASE_URL }}
          NEXTAUTH_URL: ${{ secrets.PROD_NEXTAUTH_URL }}
          NEXTAUTH_SECRET: ${{ secrets.PROD_NEXTAUTH_SECRET }}
          JWT_SECRET: ${{ secrets.PROD_JWT_SECRET }}
          CLOUDINARY_CLOUD_NAME: ${{ secrets.PROD_CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_API_KEY: ${{ secrets.PROD_CLOUDINARY_API_KEY }}
          CLOUDINARY_API_SECRET: ${{ secrets.PROD_CLOUDINARY_API_SECRET }}
          RAZORPAY_KEY_ID: ${{ secrets.PROD_RAZORPAY_KEY_ID }}
          RAZORPAY_KEY_SECRET: ${{ secrets.PROD_RAZORPAY_KEY_SECRET }}
          NEXT_PUBLIC_RAZORPAY_KEY_ID: ${{ secrets.PROD_RAZORPAY_KEY_ID }}
          NEXT_PUBLIC_SOCKET_URL: ${{ secrets.PROD_SOCKET_URL }}
          NEXT_PUBLIC_IMAGE_URL: ${{ secrets.PROD_IMAGE_URL }}
          NEXT_PUBLIC_API_URL: ${{ secrets.PROD_API_URL }}

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.PROD_AZUREAPPSERVICE_CLIENTID }}
          tenant-id: ${{ secrets.PROD_AZUREAPPSERVICE_TENANTID }}
          subscription-id: ${{ secrets.PROD_AZUREAPPSERVICE_SUBSCRIPTIONID }}

      - name: Deploy to Azure Production
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'mohaweaves-nextjs'
          slot-name: 'Production'
          package: .
