/**
 * LanguageSwitcher — placeholder for future i18n support.
 *
 * next-intl is installed but not yet configured (no locale routing,
 * no message files). This component is intentionally not rendered anywhere.
 * When i18n is ready:
 *   1. Wrap next.config.js with createNextIntlPlugin
 *   2. Add messages/ directory with locale JSON files
 *   3. Set up middleware.ts for locale detection
 *   4. Import and render this component in the Header
 */
export default function LanguageSwitcher() {
  return null;
}
